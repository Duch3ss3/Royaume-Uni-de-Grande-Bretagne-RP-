const getArticles=()=>JSON.parse(localStorage.getItem('uk2308_articles')||'[]');
function card(a,i=0){return `<article class="news-card"><div class="news-meta"><span class="category">${a.category}</span><time>${a.date}</time></div><h3>${a.title}</h3><p>${a.summary}</p></article>`}
const home=document.getElementById('home-news');if(home){const a=getArticles().slice(0,3);home.innerHTML=a.map(card).join('');}
const list=document.getElementById('news-list');if(list){const search=document.getElementById('news-search'),cat=document.getElementById('news-category');const render=()=>{const q=search.value.toLowerCase(),c=cat.value;const rows=getArticles().filter(a=>(c==='all'||a.category===c)&&(`${a.title} ${a.summary}`.toLowerCase().includes(q)));list.innerHTML=rows.length?rows.map((a,i)=>`<article class="news-card"><div class="news-index">${String(i+1).padStart(2,'0')}</div><div>${card(a,i).replace('<article class="news-card">','').replace('</article>','')}</div></article>`).join(''):'<div class="paper-panel">Aucune publication trouvée.</div>'};search.addEventListener('input',render);cat.addEventListener('change',render);render();}