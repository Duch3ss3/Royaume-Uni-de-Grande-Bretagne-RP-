const getOfficial=()=>JSON.parse(localStorage.getItem('uk2308_official')||'[]');const list=document.getElementById('official-list');const search=document.getElementById('official-search'),type=document.getElementById('official-type');function render(){const q=search.value.toLowerCase(),t=type.value;const rows=getOfficial().filter(a=>(t==='all'||a.type===t)&&(`${a.title} ${a.summary} ${a.reference}`.toLowerCase().includes(q)));list.innerHTML=rows.length?rows.map(a=>`<article class="official-item"><span class="document-type">${a.type} · ${a.reference}</span><h2>${a.title}</h2><time>${a.date}</time><p>${a.summary}</p></article>`).join(''):'<div class="paper-panel">Aucun acte publié pour le moment.</div>'}search.addEventListener('input',render);type.addEventListener('change',render);render();